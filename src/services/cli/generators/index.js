const { projectConfigurationFileGenerator } = require('./projectConfigurationFile');
const { targetHTMLGenerator } = require('./targetHTML');

module.exports = {
  targetHTMLGenerator,
  projectConfigurationFileGenerator,
};
